import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        margin: 10,
        paddingHorizontal: 10,
        borderRadius: 5,
        borderBottomWidth: 1,
        borderColor: '#D0E7FB',
        flexDirection: 'row',
        alignItems: 'center',
    },

    input: {
        color: 'white',
        flex: 1,
    },
});